project: findver-agent
current_phase: findoasis-phase4-financial-dsl
current_goal: Implement FinOASIS protocol v3 additively through credential-free local
  tests while preserving v1/v2, M2 and Official Test V2 artifacts unchanged.
last_completed_phase: findoasis-phase4-financial-dsl
last_git_commit: 56f45ffd7f9770c1a146cd00b14fa79a9b48deef
fixed_constraints:
- runtime_has_no_gold_feedback_or_scorer_access
- separate_agent_and_scorer_compose_projects
- separate_networks_mounts_and_build_contexts
- scorer_network_mode_none
- host_only_one_way_submission_handoff
- fixed_gateway_is_only_model_network_path
- one_session_per_question
- no_example_specific_answer_rules
decisions:
- preserve_original_single_call_path_as_baseline
- use_shared_text_json_action_protocol
- use_structured_question_state_and_raw_jsonl_traces
- use_dev_feedback_dev_holdout_final_hidden_split_policy
- Python 3.12 is used on Ubuntu 24.04; package metadata will support 3.11 and newer.
- The released testmini annotations are development-only and are not represented as
  final hidden evaluation.
- Runtime model requests carry no provider credential; only Model Gateway owns upstream
  authentication.
- Runtime exposes exactly search_report, read_paragraphs, calculator, and submit_answer;
  no generic file or execution skill exists.
- Both Agent and Baseline use the same text JSON submit protocol; native provider
  tool calling is excluded from the primary experiment.
- Sealed archives contain exactly predictions.jsonl, manifest.json, and SHA256SUMS
  and are chmod 0444.
- Docker remains root-controlled in WSL; the development user was not added to the
  docker group.
- Use mirror.gcr.io as the Docker Hub pull-through cache.
- The external .env.agent is sourced only at Gateway launch; secrets are never copied
  into the repository or image.
- The external .env.scorer remains unused because the deterministic scorer is networkless
  and must not hold API credentials.
- Gateway egress is direct by default; host proxy variables are not inherited.
- Use one host flock across Agent, handoff, and Scorer operations.
- Mount only the exact per-run output directory into Agent Runtime.
- Publish a history-free allowlisted Agent release; exclude legacy gold, scorer, outputs,
  and unrestricted inference paths.
- Use run one-e2e-a1-20260814 as the end-to-end gate before the paid full matrix
- Use official FinDVer commit e8bb237def4ce555a606a45edba22666e31df248 test/testmini
  embedding-3-large top-10 outputs; current tasks are covered 700/700.
- Keep formal API configs separate from legacy baseline configs and freeze temperature=1,
  top_p=1, seed=7, max_output_tokens=1024.
- Scope Git safe.directory to the matrix process environment; do not modify global
  Git configuration.
- Treat model JSON formatting failures and max-step exhaustion as experimental invalid
  results; do not retry or tune prompts.
- Freeze formal results to matrix findver-api7-formal-v1, model deepseek-v4-flash,
  and code commit 9a4144fa7059b026f7e444514f403fcca064875d
- Record the public 700-task formal matrix as development evaluation, not a final-hidden
  or leaderboard claim
- Adopt ADR 0002 RAG-seeded budget-aware evidence verification while preserving protocol
  v1 and frozen experiment history.
- Seed evidence is pinned and counts toward evidence and unique-paragraph limits but
  consumes no model, exploration, search, or read budget.
- FixedEmbeddingIndex remains a top-10 compatibility loader; new FixedRetrievalIndex
  validates independent top-3, top-5, and top-10 artifact metadata and SHA256.
- Protocol v1 retains historical max_steps behavior; protocol v2 persists phase budgets
  and rejects incompatible resume.
- Every v2 model attempt is durably charged only to its active phase before the request;
  exhausted exploration always transitions to reserved Finalization.
- Protocol v2 requires bounded control metadata on every action and persists only
  evidence status, confidence, up to five evidence gaps, and enumerated risks.
- Selective Review uses deterministic risk triggers and can replace a draft only with
  a fully valid submission; all review failures fall back to the verified draft.
- Budget-matched iterative RAG uses a fixed seed, a configured invariant count of
  query/search/auto-read rounds, and strict finalization; it has no evidence controller,
  adaptive stop, or review.
- Aggregate model-call totals count requests, while tokens and latency count completed
  responses; state is used only for aggregate tool, evidence, and review totals.
- Private evidence, recovery, paired-bootstrap, and exact McNemar analysis is implemented
  in the separate networkless Private Scorer repository at commit 37aad0d; its code,
  private data, and outputs remain outside this Agent workspace and Runtime build
  context.
- Iterative RAG uses invariant configured retrieval rounds plus submit-only finalization
  without controller/review; aggregate calls count requests while usage counts responses;
  private evidence analysis remains isolated in the separate Private Scorer repository.
- B-class main configs are paired across API and Local with identical generation and
  method settings; backend alias, timeout, retries, and backend kind are the only
  pair differences.
- The two-model planner is preparation-only, requires distinct explicit model IDs,
  creates independent run IDs, and freezes shared task/retrieval hashes and per-condition
  fairness fields.
- Top-k ablations are limited to one primary model on development data; independent
  official top-3, top-5, and top-10 artifacts from frozen FinDVer commit e8bb237 are
  available, while any actual ablation run still requires explicit authorization.
- B-class API/local pairs share method and generation settings; the planner requires
  distinct explicit model IDs and produces prepared-not-executed paired runs; paid
  and final-hidden execution remains unauthorized.
- The ultra audit was executed exactly once after all implementation phases; no paid
  or hidden experiment ran, no historical artifact changed, and no post-audit implementation
  refactor is required.
- Public release matrix tests use a minimal allowed task fixture while production
  task and retrieval artifact hashes remain pinned.
- Agent submissions carry a separate immutable evidence-ledger.jsonl sidecar containing
  only example IDs, initial RAG paragraph IDs, and final ledger paragraph IDs; its
  schema and SHA256 are bound into manifest.json.
- Formal B-class execution uses only the schema-v2 plan executor; model, code, config,
  task, retrieval, run ID, backend, context capacity, and resume identity fail closed
  on drift.
- Protocol- or skill-rejected actions consume attempts and record errors but cannot
  mutate evidence control or risk state; selective review reads the accepted draft
  risk snapshot.
- Reserve 35 percent of evidence prompt characters for up to four recent dynamic paragraphs,
  guarantee the newest two when they fit, and export only aggregate visibility rates.
- Use a 32768-token prompt-construction budget and a 100000-token B-class model context
  capacity.
- Treat context capacity as a local hash-bound constraint and never send it as a nonstandard
  API field.
- Fail before transport on estimated overflow and fail after response on authoritative
  provider-usage overflow.
- Use explicit file_completion_rate, valid_output_rate, invalid_rate, and review_trigger_rate
  keys while retaining documented legacy aliases.
- Keep the single /run/lock/findver-evaluation.lock path and open it read-only for
  flock compatibility with a root-created 0644 lock file.
- Always build model-gateway and agent-runtime from the current source before the
  generic launcher starts containers.
- Use independent official upstream top-3/top-5 files from commit e8bb237; never derive
  them by truncating the paragraph-ID-sorted top-10 artifact.
- Accept the original FinDVer report path format by reducing it to a basename and
  requiring an exact public-task report match.
- Treat bclass-stateful-local-20260817-03 as the first valid post-provenance-fix stateful
  container evidence; retain -02 only as stale-image diagnostic history.
- Validate sidecar content, exact population, and prediction order independently in
  both Agent handoff verification and Private Scorer ingestion.
- Formal Top-k API plan rows may reference configs/bclass/ablations; Local rows remain
  rejected.
- Public-release tests must use the tracked minimal public-task fixture whenever validation
  reaches frozen artifact hashing.
- Public CI copies a tracked synthetic task fixture into the ignored runtime_data/public
  path; the fixture must match a tracked public report and frozen retrieval identity.
- Public release ecd22935827b4b252ce6f878bb5d5700a79cf464 passed GitHub Actions run
  31996865142 for Python 3.11, Python 3.12, and Stateful Docker M2 smoke.
- Treat the current implementation as candidate-frozen only; allow at most one development-only
  BITER round calibration after Model A Canary, then freeze the full experiment protocol
  before holdout or hidden evaluation.
- A clean public release preflight must install .[dev,gateway], stage the tracked
  smoke fixture, and provision the ignored Gold-free formal task file from the approved
  host source.
- No paid model call, real-model Canary, second-model matrix, holdout, or final_hidden
  execution is authorized by this checkpoint.
- Allow Model B only as an all-or-none optional planner group; omit it for a seven-row
  Model-A-only plan with a matrix ID distinct from the fourteen-row paired plan.
- Freeze configured question concurrency at 32 with bounded workers, per-question
  serial phases, durable completion-order partial journals, task-order final output,
  and fail-closed resume identity.
- 'Use closed request profiles: deepseek_v4_openai requires exactly thinking.type=disabled,
  while generic_openai sends no DeepSeek field and no arbitrary request-extension
  dictionary exists.'
- Accept only stop, length, and content_filter finish reasons; count length separately
  and fail closed on unknown reasons or nonempty hidden reasoning under disabled thinking
  without persisting its value.
- Retain temperature=0, top_p=1, seed=7, and max_output_tokens=1024; truncation evidence,
  not condition-specific tuning, will govern any later ceiling proposal.
- Iterative-RAG finalization lists exact allowed evidence IDs and carries the prior
  bounded parse, protocol, skill, or unusable-response error into the next reserved
  finalization attempt.
- Keep finalization_steps=2, concurrency=32, generation parameters, strict evidence-subset
  validation, and all B-class configs unchanged; do not auto-map or silently drop
  unknown evidence IDs.
- Treat M2 as the current development point-estimate leader; its accuracy gain over
  strongest one-call BLC remains exploratory and nonsignificant before the extension
  runs.
- Use one permitted two-round BITER calibration because three-round BITER averages
  4.001 calls versus M2 at 3.260.
- Use separate schema-v2 plans for Top-3, Top-5, and BITER2 because retrieval identity
  is plan-level.
- BITER2 changes only fixed retrieval rounds from three to two and is the only permitted
  BITER calibration before freeze.
- Do not start another paid row until summarize-seal-verify is automatic and committed;
  regenerate remaining plans because plan commit binding is fail-closed.
- Run M2_BUDGET4 because 107/700 M2 examples used more than four Exploration calls.
  Do not run budget 8 because only 39/700 entered Finalization after six Exploration
  calls and none max-step terminated. Freeze M2 versus BLC as the sole primary accuracy
  comparison; Holm-adjust the five named secondary method comparisons; treat top-k,
  budget, and BITER calibration as exploratory.
- Freeze M2 as the primary candidate and BLC as the sole primary comparator on accuracy
  with two-sided exact McNemar alpha 0.05. Freeze five secondary method comparisons
  under Holm familywise alpha 0.05. Retain budget-4 only as an exploratory lower-cost
  point, do not run budget 8, and permit no further Model-A top-k, BITER-round, or
  exploration-budget tuning before holdout.
- Authorize implementation and offline verification of exactly one LC_AGENT_FIRSTPASS
  post-hoc dev_feedback extension while keeping paid execution unauthorized.
- Inject the exact full report at most once in the first durably charged Exploration
  attempt; transport retries reuse it, while later Agent attempts do not.
- Keep M2 versus BLC and the five-comparison Holm family frozen; LC results cannot
  replace M2 in the current holdout protocol.
- LC_AGENT_FIRSTPASS is identical to API A_SCRATCH except for a preview-only full
  report on the first durably charged Exploration attempt.
- Transport retries reuse the identical request; later Agent attempts never receive
  the report after the durable first-attempt claim.
- The full report never preloads the evidence ledger; final evidence still requires
  read_paragraphs and the sealed sidecar remains unchanged.
- 'LC_AGENT_FIRSTPASS passed every frozen Runtime feasibility gate: full completion,
  strict valid rate at least 99 percent, no local truncation, no provider context
  error, and input tokens below 46,376,030.'
- Accuracy, evidence quality, paired statistics, and the holdout promotion rule remain
  unevaluated because Private Scorer execution was explicitly out of scope.
- The sealed Runtime artifact remains local; no scorer inbox handoff occurred.
- LC versus A_SCRATCH was +3.286 points with 95 percent CI +0.571 to +6.000 and exact
  p=0.0220; LC versus BLC was +3.429 points with CI +0.429 to +6.571 and exact p=0.0369.
- LC versus M2 was +0.857 points with 95 percent CI -1.714 to +3.286 and exact p=0.5716,
  so the frozen LC promotion threshold was not met and M2 remains the primary candidate.
- LC submitted Evidence F1 was 58.736 percent, 2.740 points above M2, while final-ledger
  Gold recall was 65.014 percent, 10.999 points below M2; initial-RAG and initial-relative
  recovery metrics are not applicable.
- No raw scorer output, private Gold, scorer code, or per-example feedback was copied
  into the Agent workspace.
- Use an independent M2_SELECTIVE_REVIEW_32K local configuration with model_context_window_tokens=32768,
  request_profile=generic_openai, prompt_budget_tokens=28672, and concurrency=2.
- Do not run LC_AGENT_FIRSTPASS or any full-report long-context injection in this
  local-model experiment.
- All local-model plans are public-development Runtime-only and explicitly leave Scorer
  handoff, dev_holdout, and final_hidden unauthorized.
- Do not run the three-example pilot or 700-example public development set after the
  strict-validity gate failure.
- Do not loosen the JSON parser, add example-specific prompt rules, or run any long-context
  condition to mask this failure.
- Keep Qwen gates outside frozen 100K/B-class history; use identical generic_openai
  M2 method and generation settings for both models.
- Do not run 700 public tasks, long-context conditions, scorer handoff, holdout, or
  hidden evaluation.
- Allow hash-bound independent-local plans only from one-level dedicated directories
  under configs/local_models.
- Keep all Qwen runs outside frozen B-class history and stop before 700 tasks, long-context,
  or scorer handoff.
- Pause after download verification; do not start vLLM, the gateway, tunnels, JSON
  probes, smoke, pilot, throughput tests, 700 tasks, or long-context work.
- Freeze Qwen provider admission at explicit plan-bound RPM/TPM values and record
  queue wait and transport retry aggregates.
- Replicate Model A as seven Qwen main rows plus four fixed parity extensions; keep
  Qwen LC_AGENT_FIRSTPASS out of scope.
- Keep exactly one canonical model-independent copy of the seven main conditions and
  four Model-B extensions; compose them with deployment YAMLs in schema-v3 plans.
- Use API-dialect transport profiles openai_standard, deepseek_openai_chat, and dashscope_openai_chat;
  only closed adapters may generate provider-specific fields.
- Bind condition, deployment, and canonical effective-config SHA256 in schema-v3 plans;
  keep historical DeepSeek schema-v2 configs and execution as a compatibility path.
- Use one canonical model-independent copy of seven main conditions and four Model-B
  extensions.
- Allow provider-specific request fields only through closed API-dialect adapters;
  arbitrary extra_body remains forbidden.
- Add an existing-dialect model only by adding a deployment YAML; add code only for
  a new closed API dialect.
- Keep historical DeepSeek configs and schema-v2 execution as a compatibility path
  while new plans bind condition, deployment, and effective-config SHA256 under schema
  v3.
- Retain both smoke directories, generated effective config, and all temporary plans
  until the user explicitly approves cleanup.
- After explicit user approval, remove the earlier Qwen smoke and temporary planning
  artifacts while retaining the completed sealed schema-v3 smoke.
- 'User explicitly authorized all eleven 700-example Qwen3.5-27B public dev_feedback
  parity rows: seven main conditions plus RAG3_SEEDED, RAG5_SEEDED, BITER2_RAG10,
  and M2_BUDGET4.'
- The authorization includes sealed Runtime postprocessing, isolated Private Scorer
  aggregate evaluation, evidence analysis where applicable, and paired aggregate comparisons
  against the frozen Model-A results.
- LC_AGENT_FIRSTPASS, dev_holdout, final_hidden, prompt/parser tuning, per-example
  feedback, and any condition outside the frozen eleven-row parity set remain unauthorized.
- Use the schema-v3 paired main plan only to execute its seven model_b Qwen rows;
  reuse the already frozen Model-A aggregate results rather than rerunning Model A.
- Use four independent schema-v3 model_b extension plans because retrieval identity
  is plan-level; execute RAG3, RAG5, BITER2, and M2_BUDGET4 after the seven main rows.
- The formal plans bind qwen3.5-27b, dashscope_openai_chat, disabled thinking, 100000-token
  context capacity, concurrency 32, and the frozen public task/retrieval hashes.
- Retain M2_SELECTIVE_REVIEW as the Qwen primary operating point; retain M2_BUDGET4
  as the lower-cost point and BITER2_RAG10 as the low-latency iterative option; do
  not tune from these read development results.
- Public smoke verification asserts the canonical deepseek_openai_chat trace identity
  while historical configuration aliases remain supported at load time.
- Add only BBM25_10 and BHYBRID_RRF10 as isolated control conditions using the exact
  BRAG10 prompt, generation settings, parser, scorer contract, concurrency, and one-call
  budget.
- Freeze Hybrid to embedding Top-10 plus BM25 Top-10 with RRF k=60, deterministic
  symmetric tie-breaking, paragraph-ID deduplication, fused Top-10, and final document
  order.
- Preserve the completed seven-condition and extension matrices; bind each new control
  in its own schema-v3 plan and require explicit approval immediately before API execution.
- 'M2 clearly outperforms BHYBRID_RRF10 on Model-A development data: 82.29 versus
  77.71 percent, +4.57 points, paired 95 percent CI +1.43 to +7.71, exact p=0.005536.'
- BBM25_10 does not outperform embedding BRAG10, while Hybrid improves over both single
  retrievers but remains clearly below M2; proceed to the planned Model-B stability
  stage without retrieval or prompt tuning.
- Keep official test unopened and preserve RRF k=60, Top-10 cutoffs, tie rule, prompt,
  generation settings, scorer, and all completed historical rows.
- Use a separate qwen3_5_27b_dashscope_stable deployment; retain all historical Qwen
  deployment files, plans, runs, and scores unchanged.
- Rerun M2 with BLC, BRAG10, BBM25_10, and BHYBRID_RRF10 because the effective Model-B
  deployment configuration changed.
- Execute only five Model-B stability conditions in order BRAG10, BBM25_10, BHYBRID_RRF10,
  M2, then BLC; leave the unselected canonical plan rows unexecuted.
- Regenerate ignored execution plans only after the final tracked planning commit,
  because the executor requires plan code_commit to equal clean HEAD exactly.
- Preserve the aborted matrix-V1 BRAG10 partial output as an unscored local transport
  diagnostic and use matrix V2 for the corrected BRAG10, M2, and BLC runs.
- 'Model-B stability gate passed: M2 is 81.71 percent versus 71.29 BRAG10 and 74.29
  Hybrid; M2-minus-BRAG10 is +10.43 points, 95 percent CI +7.14 to +13.71, exact p=9.91e-10,
  and M2-minus-Hybrid is +7.43 points, 95 percent CI +4.29 to +10.71, exact p=0.0000131.'
- Proceed only to a single V2 freeze plan/ADR after explicit user authorization; official-test
  access and project-contract changes remain unauthorized.
- Public retrieval-control plan tests use the tracked Gold-free minimal task fixture
  in an isolated temporary release tree; formal task paths and hashes remain unchanged.
- Freeze official-test V2 to Model A and exactly BRAG10, BBM25_10, BHYBRID_RRF10,
  BLC, and M2, with M2 versus BRAG10 as the sole primary accuracy comparison and M2
  versus Hybrid as the key secondary control.
- Use immutable ref findver-official-test-v2-freeze, FinDVer data commit e8bb237,
  deepseek-v4-flash, the frozen generation and RRF settings, and Private Scorer commit
  6ec342; keep execution unauthorized until separate input-binding and API approvals.
- Preserve the historical V1 plan and project contract without modification.
- Implement protocol v3 in an isolated findver_agent.findoasis package and dispatch
  before legacy state or prompt logic.
- Preserve v1/v2 actions, state, prompt, M2, Official Test V2, sealed archive, and
  evidence sidecar unchanged.
- Use ADR 0011 because Draft PR 1 occupies ADR 0010.
- Only Runtime SkillResult application may satisfy an obligation; model deltas cannot
  satisfy or waive obligations.
- Protocol v3 uses an independent schema-v3 state store with config, Registry, policy,
  report, and optional corpus identity binding.
- Official Test V2, M2, sealed archive, evidence sidecar, and canonical v1/v2 state
  and prompt outputs are now hash-frozen by tests.
- Seed obligations from claim text only, expose only the Registry subset justified
  by active obligations and trusted Runtime facts, and charge every model attempt
  before transport.
- Store exact read report evidence with a matching SHA256; prompts may show only a
  bounded untrusted view of ledgered text and search candidates.
- Dispatch protocol v3 before constructing legacy StateStore, PromptBuilder, or fixed
  retrieval objects. Unavailable v3 calls record rejection but cannot apply model
  control metadata or mutate proof ledgers.
- Protocol v3 dispatches before legacy StateStore, PromptBuilder, or fixed retrieval
  construction.
- Only exact hash-checked ledger evidence and bounded search snippets are visible
  as untrusted prompt data.
- Index table HTML additively by existing context position without altering legacy
  paragraph IDs or text.
- Persist exact selected cell text and coordinates, but never raw HTML, in protocol-v3
  state and prompts.
- Use deterministic table unit and scale inference only to fill unknown model metadata
  or reject conflicts; never infer numeric text.
- Bind all numeric values as immutable Decimal strings to one unique exact evidence
  occurrence.
- Represent FinDSL as a maximum-depth-4 typed AST with at most 32 nodes and 32 total
  reference leaves; raw numeric operands are forbidden.
- Use Decimal with precision 50, percentage-point internal representation, and 100
  basis points per percentage point.
- Persist canonical AST and claim relation so program hashes, leaf refs, snapshots,
  evidence refs, and certificate payload hashes are independently replay-verifiable
  on resume.
- Bind ISO dates and booleans exactly for type-safe comparisons only; arithmetic remains
  Decimal-only.
- Treat a verified false relation as a valid numeric certificate outcome while keeping
  relation_satisfied explicit for final claim verification.
files_changed:
- src/findver_agent/financial_dsl/
- src/findver_agent/findoasis/actions.py
- src/findver_agent/findoasis/agent.py
- src/findver_agent/findoasis/prompt_builder.py
- src/findver_agent/findoasis/state.py
- src/findver_agent/findoasis/value_binding.py
- tests/integration/test_finoasis_router.py
- tests/integration/test_finoasis_table_value.py
- tests/unit/test_actions_v3.py
- tests/unit/test_financial_dsl_v3.py
- tests/unit/test_prompt_v3.py
- tests/unit/test_value_binding_v3.py
tests_passed:
- 9 unit tests
- three JSON Schemas validate
- 700 public tasks contain only allowed fields
- 15 unit tests
- 32 unit tests
- 39 unit and integration tests
- all four configs load and CLI help succeeds
- 47 unit, integration, and security tests
- deterministic archive and runtime context allowlist checks
- 55 pytest tests; Compose expands; both images build; container hardening metadata
  matches the contract.
- 61 Agent tests and 10 independent Scorer tests
- Mock API/local Docker paths, real API direct Gateway path, one-way handoff, dev
  scoring, and aggregate-only final mode
- 68 Agent tests and 10 independent Scorer tests
- Rebuilt Agent, Gateway, and Scorer images; post-audit Mock Agent and aggregate-only
  Scorer runs passed
- Two real API direct-egress smoke runs completed without inherited host proxy variables
- 69 Agent tests and 10 independent Scorer tests
- Agent, Gateway, and Scorer images rebuilt from their separate build contexts
- One-question real API A1 run completed with 3 model calls and a valid prediction
- Sealed three-file archive verified, handed off read-only, and scored in dev-detailed
  and final-aggregate modes
- Exact API credential and base URL markers were absent from all run artifacts
- 81 Agent unit, integration, and security tests
- 10 independent Private Scorer tests
- Agent, Gateway, and Scorer images rebuilt; Compose expands
- Runtime container loaded all seven API configs and 700 B3 records with retrieval
  mount RW=false
- 81 Agent tests after the root-launch fix
- 21 real API Pilot instances completed and archived
- B0/B1 full-report requests succeeded without silent truncation or context errors
- B2/B3 contexts ran; A0 calculator calls=0; A2 review gate completed on valid submissions
- Pilot resume skipped all completed conditions; scorer inbox empty; Agent containers
  stopped
- Formal API matrix completed all seven conditions at 700/700 with archived status
  and no condition errors
- Seven sealed submissions were aggregate-only scored and privately archived; Scorer
  inbox is empty and no experiment containers remain
- 81 Agent tests and 10 independent Private Scorer tests passed after formal completion
- Formal aggregate JSON/Markdown and tracked experiment report record accuracy, coverage,
  invalid counts, comparisons, and runtime behavior
- 81 Agent tests passed; one existing Starlette deprecation warning.
- 35 targeted fixed-retrieval, config, state, Agent compatibility, and RAG Seed tests
  passed.
- 105 full Agent tests passed; one existing Starlette deprecation warning.
- 29 targeted staged-budget, finalization, review-budget, resume, prompt, and v1 compatibility
  tests passed.
- 117 full Agent tests passed; one existing Starlette deprecation warning.
- 47 targeted Action control, Prompt, Selective Review, staged-budget, and v1 compatibility
  tests passed.
- 141 full Agent tests passed; one existing Starlette deprecation warning.
- 18 targeted Phase 4 tests and 149 full Agent tests passed; one existing Starlette
  deprecation warning.
- 24 targeted config, matrix, launcher, mock, compatibility, and isolation tests plus
  158 full Agent tests passed; API/Local Compose expanded, Runtime/Gateway images
  rebuilt, all 17 container configs loaded, and Mock API, Mock Local, and iterative
  container runs completed.
- 700 retrieval records and 106 Runtime files validated; 80 focused Agent audit tests,
  158 full Agent tests, and 10 unchanged Private Scorer tests passed; three-file mock
  submission and Compose isolation verified.
- 159 full Agent tests passed; one existing Starlette deprecation warning.
- 161 full Agent tests passed; one existing Starlette deprecation warning.
- 169 full Agent tests passed; one existing Starlette deprecation warning.
- B-class planner/executor CLI help, launcher shell syntax, Ruff, and diff checks
  passed.
- 29 focused review, budget, resume, state, and prompt tests passed.
- 170 full Agent tests passed; one existing Starlette deprecation warning.
- Python compileall and diff checks passed.
- 16 focused prompt, summary, RAG-seed, and Agent-loop tests passed.
- 171 full Agent tests passed; one existing Starlette deprecation warning.
- 25 focused context, backend, plan, baseline, and summary tests passed.
- 176 full Agent tests passed; one existing Starlette deprecation warning.
- Python compileall, shell syntax, CLI help, and diff checks passed; Black is not
  installed.
- 178 Agent tests passed; one existing Starlette deprecation warning.
- Python compileall, all shell syntax checks, CLI/static workflow checks, security
  lock checks, and git diff checks passed.
- 18 focused launcher, isolation, selective-review, and mock-protocol tests passed.
- 178 full Agent tests passed; one existing Starlette deprecation warning; shell syntax
  and diff checks passed.
- 35 focused fixed-retrieval, B-class config, and Runtime-bundle security tests passed.
- 181 full Agent tests passed; one existing Starlette deprecation warning; diff checks
  passed.
- Official top-3 artifact SHA256 4c85f4cc3ea07c45ae6320032f0bad34b6f095aa8751a84f3ca0fe423e5ac8d7
  and top-5 SHA256 78bce403b92d96858df689c15fb9afc3dd6b19a139d57b953e391ccb2f7d358d
  each validated 700 records with exact cutoff length.
- 'User-side bclass-stateful-local-20260817-03 rebuilt both images and passed strict
  offline verification: actions=8, model_calls=9, termination_reason=review_fallback.'
- 19 Private Scorer tests passed; both networkless Compose profiles expanded and the
  isolated image built.
- Both scorer CLIs started in read-only no-network containers; image contents contained
  only scorer code and public contracts.
- 181 Agent tests passed with one existing Starlette deprecation warning; diff checks
  passed.
- 12 focused submission and sidecar tests passed.
- 185 Agent tests passed with one existing Starlette deprecation warning; Python syntax
  and diff checks passed.
- 8 focused executor tests; 187 full Agent tests; Python compileall and git diff checks.
- 7 focused matrix-planner tests; 187 full Agent tests; Python compileall and git
  diff checks.
- 12 focused CI/public-data/runtime-bundle tests; 188 full Agent tests; shell syntax,
  Python compileall, and git diff checks.
- 188 full Agent tests passed in the development workspace; one existing Starlette
  deprecation warning.
- Clean public release ecd2293 passed 188 tests, compileall, shell syntax, entrypoint
  help, public-task validation, and API Compose expansion.
- 'Clean public release stateful Docker M2 smoke passed on the third infrastructure
  attempt: actions=8, model_calls=9, termination_reason=review_fallback; the first
  two builds stopped on transient PyPI read timeouts before Runtime start.'
- Private Scorer 37aad0d passed 19 tests, both networkless Compose profiles, isolated
  image builds, both hardened CLI starts, and image-content inspection.
- The 700-task public input and Top-10 retrieval matched their frozen SHA256 values;
  a synthetic preparation-only plan validated schema v2, prepared_not_executed status,
  and 14 unique run IDs.
- 'Pre-change recovery suite: 188 Agent tests passed with one existing Starlette deprecation
  warning.'
- 'Final directed single-model, executor drift, concurrency/resume/order, thinking,
  finish-reason, summary, and sealing suite: 81 tests passed with the same warning.'
- 'Full post-upgrade Agent suite: 225 tests passed with the same warning; compileall,
  all shell syntax, CLI help, diff check, and API/Local Compose expansion passed.'
- Runtime and Gateway images rebuilt from separate allowlisted contexts; sensitive-marker
  and image-content scans passed.
- Stateful M2 Docker smoke retained 8 actions, 9 model calls, and review_fallback;
  40-task concurrent Docker smoke reached configured/effective/peak 32/32/32, preserved
  task order and sidecar order, sealed successfully, and reported 40 stop with zero
  length or protocol drift.
- Preparation-only Model A plan validated schema v2, prepared_not_executed, seven
  unique rows, a distinct single-model matrix ID, exact disabled-thinking provenance,
  and concurrency 32.
- 'Pre-change full Agent suite: 225 tests passed with one existing Starlette deprecation
  warning.'
- 'Focused iterative-RAG regression suite: 3 tests passed.'
- 'Post-change full Agent suite: 226 tests passed with the same warning; compileall
  and git diff checks passed.'
- 226 Agent tests passed with one existing Starlette deprecation warning.
- Private Scorer commits 6cc5a13 and 4bbcc70 passed 26 tests, all three networkless
  Compose profiles, isolated image build, and hardened CLI checks.
- Seven 700-example aggregate summaries, six 10,000-resample paired comparisons, and
  one Top-10 evidence analysis completed in the networkless scorer.
- 22 focused extension planner, B-class config, and formal executor tests passed.
- 232 full Agent tests passed with one existing Starlette deprecation warning.
- Python compileall, all shell syntax checks, CLI help, and git diff checks passed.
- 15 formal executor unit tests passed; 33 focused postprocessing/submission/summary
  tests passed; 234 full Agent tests passed with one existing Starlette deprecation
  warning.
- 26 focused configuration/planner/executor tests passed; 236 full Agent tests passed
  with one existing Starlette deprecation warning; compileall and git diff checks
  passed.
- 236 full Agent tests passed with one existing Starlette deprecation warning; 26
  Private Scorer tests passed; all four extension archives verified and are immutable
  mode 0444; no evaluation containers remain active; git diff check passed.
- 'Pre-change recovery suite: 236 Agent tests passed with one existing Starlette deprecation
  warning.'
- Protocol documentation diff check passed.
- 248 pytest tests passed.
- 700-task offline LC preflight completed with 700 first-pass injections, zero model
  requests, and zero estimated context overflows.
- Stateful Mock Docker run completed with 9 calls and exactly one Exploration-attempt-1
  full-report injection; cleanup verified.
- Python compileall, shell syntax, and git diff checks passed.
- Pre-run public-data verification covered 700 tasks and all 248 pytest tests passed
  on commit 1fff62e.
- The formal schema-v2 executor completed 700/700 examples with strict valid rate
  0.995714, 39,268,332 input tokens, 303,935 output tokens, zero local truncations,
  and zero provider context errors.
- Long-context telemetry recorded exactly 700 injections, all in Exploration attempt
  1, across 2,803 model requests.
- The sealed archive independently verified with 700 predictions, the bound evidence
  sidecar, exact three-file contents, and SHA256 9c14142c0609f6cf46e8f1ea44da7b31211022103813049bbc169850108cca4f.
- Runtime and Gateway containers and networks were removed and the global evaluation
  lock was released.
- Private Scorer final-aggregate mode scored 700 examples with 582 correct, 697 valid,
  and accuracy 0.831429; no per-example feedback artifact was produced.
- Three 700-example paired comparisons completed with 10,000 bootstrap resamples and
  exact two-sided McNemar tests.
- The no-initial-retrieval evidence profile completed aggregate-only analysis after
  28 Scorer tests, all Compose profile checks, schema validation, and Private Scorer
  commit d665d1f.
- Agent and Scorer containers were absent after scoring, both repositories were clean,
  and no holdout or hidden input was used.
- 27 focused local-32K config, planner, executor, and frozen B-class tests passed.
- 253 full Agent tests passed with one existing Starlette deprecation warning.
- Python compileall, launcher shell syntax, planner CLI help, plan-generation smoke,
  and git diff checks passed.
- Two RTX 3090 replicas, 32K vLLM backends, load-balancing gateway, WSL tunnel path,
  and project Model Gateway path returned HTTP 200 with the expected model name.
- One-example Runtime run completed file output with 8 stop responses, zero length
  finishes, zero protocol drift, zero local truncations, zero provider context errors,
  and zero 5xx or timeouts.
- The smoke run was summarized, sealed, and verified; Agent containers, networks,
  SSH tunnel, remote gateway, and remote model replicas were stopped.
- 6 focused local-model configuration and planner tests passed.
- 255 full Agent tests passed with one existing Starlette deprecation warning.
- 23 focused local-plan executor and configuration tests passed.
- Qwen2.5-7B-Instruct revision a09a35458c702b33eeacc393d103063234e8bc28 has config,
  index, four present shards, and zero broken links.
- Qwen2.5-14B-Instruct revision cf98f3b3bbb457ad9e2bb7baf9a0125b6b88caa8 has config,
  index, eight present shards, and zero broken links.
- Both download containers exited 0; no Qwen service container or ports 8000, 8001,
  or 18080 are active.
- 'Pre-change recovery suite: 255 tests passed with one existing Starlette deprecation
  warning.'
- 'Qwen transport, gateway, rate-limit, retry, config, and summary suite: 50 tests
  passed.'
- 'Full post-transport Agent suite: 268 tests passed with the same warning; compileall
  and diff checks passed.'
- 'Qwen B-class configuration, matrix, extension, executor, config, and runner suite:
  77 tests passed.'
- Offline frozen-artifact generation validated 14 main rows and four separate Qwen
  Model-B extension plans.
- 'Full post-planner Agent suite: 281 tests passed with one existing Starlette deprecation
  warning; compileall, CLI help, and diff checks passed.'
- 'Full Agent suite: 270 tests passed with one existing Starlette deprecation warning.'
- 'Offline schema-v3 composition: 14 paired main rows plus four Qwen extension rows;
  all condition, deployment, and effective-config SHA256 bindings recomposed successfully.'
- compileall, CLI help, and git diff checks passed.
- 'Committed-plan executor binding: qwen3.5-27b, dashscope_openai_chat, disabled thinking,
  and effective config SHA256 2e381d29092d62d21997a3651af6e9e481a79e43637d31f5d8e036635091f210.'
- 'Gold-free Qwen BLC smoke: 3/3 file completion, 3/3 strict valid, 3 stop responses,
  zero retries, zero protocol drift, and sealed archive SHA256 4d4dcff4b922029281c8071ca6b1261e72c7fd412b05233e7ec64f13c86dea0c.'
- Cleanup verification confirmed the early smoke and effective-config cache are absent,
  the sealed composable smoke remains complete, the tracked worktree is clean, and
  no Docker service is running.
- 'Pre-authorization recovery suite: 270 tests passed with one existing Starlette
  deprecation warning; worktree was clean and no Docker service was running.'
- 270 pytest tests, compileall, launcher shell syntax, 700-record public-data validation,
  exact task and Top-3/Top-5/Top-10 hashes, API Compose expansion, credential mode/model
  identity, and empty Docker service state passed.
- 270 Agent tests, compileall, launcher shell syntax, git diff checks, 30 Private
  Scorer tests, all three networkless Scorer Compose profiles, 11 sealed-submission
  verifications, byte-for-byte private archive checks, empty scorer inbox, and empty
  Agent/Scorer container state passed.
- 270 Agent tests
- 'Stateful Docker M2 smoke: 9 calls, 8 actions, review fallback verified'
- 'Concurrent Docker smoke: 40 examples, configured and peak concurrency 32'
- compileall, shell syntax, and git diff checks
- 280 Agent tests passed with one existing Starlette deprecation warning.
- BM25 artifact matched official e8bb237 testmini Top-10 for all 700 tasks; Hybrid
  RRF artifact validated k=60, Top-10 inputs, deduplication, Top-10 output, and document
  order.
- Python compileall, launcher shell syntax, and git diff checks passed.
- Both Model-A retrieval-control runs completed 700/700 with 100 percent valid output,
  exactly 700 calls each, zero retries, zero length finishes, zero protocol drift,
  zero provider context errors, and zero local truncations.
- Both sealed submissions independently verified; networkless Private Scorer produced
  two aggregate summaries and five 10,000-resample paired comparisons; scorer inbox
  and all Agent/Scorer containers are empty.
- 280 Agent tests passed with one existing Starlette deprecation warning; compileall
  and git diff checks passed.
- 284 Agent tests passed with one existing Starlette deprecation warning.
- The 14-row offline composition smoke bound json_object, max_retries 10, 240 RPM,
  and 400000 TPM for the selected Model-B BLC, BRAG10, and M2 rows.
- Python compileall and git diff checks passed.
- All five selected plan rows recomposed successfully against qwen3.5-27b from .env.agent,
  the clean committed worktree, exact task/retrieval hashes, json_object response
  mode, max_retries 10, 240 RPM, and 400000 TPM.
- 284 Agent tests, Python compileall, git diff checks, and five selected offline executor
  recompositions passed for the stable Qwen deployment.
- 62 focused gateway, backend, config, and deployment tests passed with one existing
  warning.
- 289 full Agent tests passed with one existing Starlette deprecation warning; compileall
  and git diff checks passed.
- All five Model-B conditions completed and sealed 700/700; archive hashes verified
  byte-for-byte at the scorer boundary.
- Networkless Private Scorer produced five final-aggregate summaries and four 10,000-resample
  paired comparisons; inbox and Agent/Scorer container state are empty.
- 30 independent Private Scorer tests passed.
- 8 focused retrieval-control tests passed.
- 289 full Agent tests passed with one existing Starlette deprecation warning.
- Python diff checks passed.
- The V2 YAML parsed and validated exactly five ordered conditions, frozen deployment/condition
  hashes, a 1,700-example population, and all execution gates false.
- Git diff verification confirmed docs/PROJECT_CONTRACT.md and the historical docs/EXPERIMENT_PLAN.md
  are unchanged.
- 289 baseline tests passed on Python 3.12
- compileall and git diff checks passed
- 93 focused v3 and compatibility tests passed
- 341 full Agent tests passed on Python 3.12
- 391 full Agent tests passed on Python 3.12
- Focused v3 routing and resume integration tests passed
- Python compileall and git diff checks passed
- 455 full Agent tests passed on Python 3.12
- 90 focused Phase 3 tests passed
- 499 full Agent tests passed on Python 3.12
- 114 focused Phase 4 action, binding, FinDSL, prompt, state, routing, and integration
  tests passed
tests_failed:
- pytest unavailable before development environment bootstrap
- None.
- 'Single-example valid_output_rate was 0: all 6 Exploration and 2 Finalization responses
  failed the unchanged strict JSON action parser, terminating with finalization_budget_exhausted.'
known_issues:
- repository_uses_python_3_12_while_original_instruction_mentions_python_3_11
- Original baseline reads gold-bearing datasets and uses LLM-based output extraction;
  only its prompt semantics will be retained.
- One upstream Starlette deprecation warning.
- One upstream Starlette TestClient deprecation warning remains.
- The strongest sealed one-call baseline uses temperature=1, while the frozen Top-3/Top-5
  candidates use temperature=0; no API call was made.
- Top3 Runtime completed 700 examples under commit 0233a48, but the prior executor
  omitted documented postprocessing; it was recovered with the existing standard tools
  and sealed as affc8d7bea57a853deb8ac3dc078a315945bde27a6283e480a627bcf2cffab26.
- DeepSeek-R1-Distill-Llama-8B under the current vLLM service emits visible reasoning
  markers before JSON in every smoke response; the service was not configured to separate
  reasoning from content.
- Qwen BLC and BRAG10 one-call rows had 305 and 205 failed model responses respectively;
  their immutable formal results are transport-degraded and require separate authorization
  before reliability investigation.
- Historical Qwen BRAG10 had 180 terminal HTTP 429 failures; historical BLC had 276
  strict JSON extra-data failures and 29 terminal HTTP 429 failures.
- The first approved V1 BRAG10 launch produced 205 partial invalid rows because the
  local Model Gateway rejected response_format with HTTP 422; the launch was stopped
  and none of those requests reached the upstream model.
- Model-B BLC required 410 recovered transient retries and produced 11 length-finished
  invalid predictions; it still completed 700/700 with 98.43 percent valid output
  and zero terminal transport failures.
- Exact official task, report, embedding, BM25, and Hybrid artifact hashes are intentionally
  unbound; this blocks plan preparation and execution until separately authorized
  input access.
- Python 3.11 is unavailable locally; compatibility will be verified by CI.
- Sandboxed Starlette TestClient threads hang; local pytest must run outside the sandbox
  with repository-scoped basetemp.
- Python 3.11 is unavailable locally and remains a CI verification item.
- Python 3.11 remains unavailable locally; repository CI must exercise that supported
  version.
next_actions:
- Implement Phase 5 frozen offline financial rule corpus, Knowledge Skills, and applicability
  certificates.
blocked_items: []
last_updated: '2026-08-28T02:37:51+08:00'
